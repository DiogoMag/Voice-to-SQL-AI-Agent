# Core dependencies
python-telegram-bot>=20.0
openai>=1.0.0
openai-whisper>=20231117
requests>=2.31.0

# Redshift connectivity (choose one)
# Option A: AWS Official Redshift connector (recommended)
redshift-connector>=2.1.0

# Option B: PostgreSQL-compatible (alternative)
# psycopg2-binary>=2.9.0
